import { Plugin } from "vuepress/core";
import { PluginSimple } from "markdown-it";

//#region src/node/markdownFileTreePlugin.d.ts
/**
 * Markdown File Tree Plugin
 *
 * This plugin is used to parse and render Markdown file trees in VuePress.
 *
 * Markdown 文件树插件
 *
 * 该插件用于在 VuePress 中解析和渲染 Markdown 文件树。
 *
 * @example
 *   import { markdownFileTreePlugin } from '@vuepress/plugin-markdown-file-tree'
 *
 *   export default {
 *     plugins: [markdownFileTreePlugin()],
 *   }
 */
declare const markdownFileTreePlugin: () => Plugin;
//#endregion
//#region src/node/fileTree.d.ts
declare const fileTree: PluginSimple;
//#endregion
//#region src/node/types.d.ts
interface FileTreeNode extends FileTreeNodeProps {
  level: number;
  children: FileTreeNode[];
}
interface FileTreeNodeProps {
  filename: string;
  comment?: string;
  focus?: boolean;
  expanded?: boolean;
  type: 'file' | 'folder';
  diff?: 'add' | 'remove';
  level?: number;
}
//#endregion
//#region src/node/parseFileTreeContent.d.ts
/**
 * Parse the info string of a single node to extract attributes such as file
 * name, comments, and type.
 *
 * 解析单个节点的信息字符串以提取属性，如文件名、注释和类型。
 *
 * @param rawInfo Node description string. 节点描述字符串
 * @returns File tree node attributes. 文件树节点属性
 */
declare const parseFileTreeNodeInfo: (rawInfo: string) => FileTreeNodeProps;
/**
 * Parse the original file tree content into a node tree structure
 *
 * 解析原始文件树内容，将其转换为节点树结构。
 *
 * @param content The original text content of the file tree. 文件树的原始文本内容
 * @returns An array of file tree nodes. 文件树节点数组
 */
declare const parseFileTreeContent: (content: string) => FileTreeNode[];
//#endregion
export { FileTreeNode, FileTreeNodeProps, fileTree, markdownFileTreePlugin, parseFileTreeContent, parseFileTreeNodeInfo };
//# sourceMappingURL=index.d.ts.map